# Claude Code 项目说明

## 项目概览
- 项目名称：<填写项目名称>
- 目标与范围：<一句话说明项目目标>
- 技术栈：<列出主要语言、框架、运行时>

## 常用命令
- 依赖安装：<例如 npm install / pnpm install / pip install -r requirements.txt>
- 本地开发：<例如 npm run dev / pnpm dev / python -m app>
- 构建打包：<例如 npm run build / pnpm build>
- 单元测试：<例如 npm test / pnpm test / pytest>
- 代码检查：<例如 npm run lint / pnpm lint / ruff>
- 类型检查：<例如 npm run typecheck / pyright>

## 目录结构
- <目录/路径>：<用途>
- <目录/路径>：<用途>
- <目录/路径>：<用途>

## 架构与关键流程
- 主要模块：<模块名与职责>
- 关键流程：<请求/任务的核心路径>
- 集成点：<外部服务或依赖>

## 代码规范
- 语言与风格：<格式化工具/风格要求>
- 命名约定：<类/函数/文件命名规则>
- 组件/模块组织：<分层或边界规则>

## 测试说明
- 测试位置：<测试目录或约定>
- 运行方式：<命令>
- 覆盖范围：<关键覆盖点>

## 变更与发布
- 版本策略：<版本号规则>
- 发布流程：<构建、打包、发布步骤>
- 回滚方式：<回滚策略>

## 注意事项
- <项目特有的约束或坑>
